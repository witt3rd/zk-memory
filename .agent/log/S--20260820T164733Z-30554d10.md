---
type: S
id: 30554d10
ts: 2026-08-20T16:47:33Z
from: zk-memory
subject: state snapshot
---

# State

- Updated: 2026-08-20T16:47:33Z
- Identity: zk-memory
- Repo: /home/dt/src/witt3rd/zk-memory.wt/feat--inhabit-signalling

## Latest handoff
- 2026-08-20T16:47:33Z — Retire HANDOFF.md; inhabit repo on the signalling protocol
# State
- **main** at origin tip; this worktree (feat/inhabit-signalling) retires HANDOFF.md in favor
  of the signalling event log. Primary clone clean.
- **100 passed, 7 skipped** (`.venv/bin/python -m pytest`; the 7 skipped are the optional
  lancedb/faiss vector+index backends not installed in the test venv). Venv: `uv venv .venv` +
  `uv pip install -e . pytest`.
- `AGENTS.md` (charter; README→symlink) + `PRINCIPLES.md` (Z-principles) + `skills/` current.
## What this is
The **host-agnostic library half** of the split (same shape as `prospecta`/`hermes-prospecta`):
a zettelkasten memory with zero Hermes / `agent.*` / LLM-provider imports. The Hermes plugin
`hermes-zk-memory` is a thin `MemoryProvider` adapter over it, pinned to `@v0.4.0`.
## Design spine (see PRINCIPLES.md)
- **Z1** write-time judgment (distill → search → judge_merge → write/merge).
- **Z2** append-only merge + collision-safe write.
- **Z9** shared/multi-host corpora: `backend="rg"`, `source=` attribution, single caretaker.
- **Z10** capture fast, integrate later: `Memory.tend_writes()` gardener (merge + retire to
  `.archive/`, or link); decisions never merge.
- **P2/P3/P9** standalone-first, LLM as injected callable, thin adapter.
## Version history
- v0.1.0 split · v0.2.0 shared-corpus (rg backend + source) · v0.3.0 `decision` kind ·
  v0.4.0 `tend_writes` gardener.
- Since v0.4.0, merged on main (by other caretaker sessions, not documented here):
  index-provider abstraction (pluggable rg/lancedb/other engine) + EmbeddingProvider protocol
  and a FAISS VectorProvider (vector recall seam). These landed without a handoff — verify the
  current surface before assuming v0.4.0 shape.
## Where I left off
This worktree only: inhabit the repo (scripts/agent + event log), retire HANDOFF.md. No other
open work in the branch.
## Gotchas
- `Memory.search` arg order — `corpus.search(query, root, ...)`; `Memory.search` wraps it.
- `judge.py` never swallows exceptions — `StructuredLLM` contracts "Never raise"; pipeline
  try/except in `retain.py`/`tend.py` is the safety net.
- `linlink mint` strips frontmatter to just `uuid:` — re-ensure title/date/kind/author after.
- `examples/*_demo.py` must stay runnable.
- Backticks in `git commit -m` get shell-substituted — use `-F file`.
## Next
1. Merge this inhabitation PR; then the repo is on the signalling protocol (no HANDOFF.md —
   use `scripts/agent handoff`).
2. Fleet shared-corpus deployment (NAS mount, setgid perms, one caretaker host running
   `tend_writes` on a schedule) — fleet-ops work.
3. Optional later: CLI `retain`/`tend-writes` with an `--llm`; PyPI publish.

## Open inbound (0)

## Open outbound (0)
